@ECHO OFF
ECHO setup and main script requires Python to work. please install that first if it isnt already
pip install requests
pip install selenium
pip install chromedriver-py
pip install pyautogui
